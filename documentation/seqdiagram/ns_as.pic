.PS
copy "sequence.pic"

#boxwid = 1
movewid = .8

# Object definition
object(MB,"M.Broker")
object(DB,"MongoDB")
object(NSAS,"NS-AS")
object(AS,"AppServer")
step

message(AS,NSAS)
message(NSAS,DB)
return_message(DB,NSAS)
step
message(NSAS,MB,"/queue/messages")

# End diagram
step
complete(MB)
complete(DB)
complete(NSAS)
complete(AS)

.PE