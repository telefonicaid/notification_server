language: "node_js"

node_js:
  - 0.8
  
services:
  - rabbitmq # will start rabbitmq-server
  - mongodb # will start mongodb

branches:
  only:
    - develop

before_install:
  - npm rebuild

before_script:
  - cd src
  - nohup node start.js > /dev/null &
  - sleep 5
  - cd ../test
  - node E2Etest.js
  - cd ..

notifications:
   email:
      - gll@tid.es
      - frsela@tid.es
