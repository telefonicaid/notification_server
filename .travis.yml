language: "node_js"

node_js:
  - 0.8

services:
  - rabbitmq # will start rabbitmq-server
  - mongodb # will start mongodb

branches:
  only:
    - develop

before_install:
  - npm rebuild

before_script:
  - cd src
  - sudo nohup node start.js &
  - sleep 5
  - cd ../test
  - node E2Etest.js
  - cd ..

after_script:
  - sudo pkill node

notifications:
   email:
      - gll@tid.es
      - frsela@tid.es
