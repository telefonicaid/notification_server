language: "node_js"

node_js:
  - 0.8

services:
  - rabbitmq # will start rabbitmq-server
  - mongodb # will start mongodb

branches:
  only:
    - develop
    - master

before_install:
  - npm rebuild
  - sudo mkdir -p /var/log/push_server/ # To store log files

before_script:
  - cd src
  - nohup node start.js > /dev/null & # Start the server
  - sleep 5 # Wait for everything to settle down
  - cd ..

script:
  - cd test/functions
  - node getToken.js # Get tokens test (via HTTP)
  # Add more tests in between
  - node E2E.js # E2E test
  - cd ../..
  - npm test # Unit tests

notifications:
   email:
      - gll@tid.es
      - frsela@tid.es
